{
// Vea https://go.microsoft.com/fwlink/?LinkId=733558 
    // para la documentación acerca del formato tasks.json
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "make project",
            "command": "make -j 16",
            "args": [
            ],
            "options": {
                "cwd": "${workspaceRoot}",
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}